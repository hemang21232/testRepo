require:

Django==4.0.2
django-timezone-field==5.0

channels==3.0.2

celery==5.2.7
django-celery-beat==2.4.0
django-celery-beat==2.4.0

redis==4.3.4
aioredis==1.3.1
channels-redis==3.3.1



pip install django, channels, redis, celery, django-celery-beat, django-celery-results


$ sudo ps -ef | grep redis
	=> redis 483 /usr/bin/redis-server
$ netstat -ltnp | grep -w '127.0.0.1:6379'

$ sudo kill -9 483
$ sudo service redis-server stop


$ ./manage.py runserver 192.168.100.xxx:8000

$ redis-server --port 127.0.0.1:6379

$ celery -A icatecd worker --beat -l info




